package scanner

// RunScanner
func RunScanner() string {
	//kafka consumer

	//run anchore
	//timeout, write to redis, job id status is failed
	//write to mysql, job data
	//write to redis, job status

	return ""
}
