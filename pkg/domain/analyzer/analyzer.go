package analyzer

// RunAnalyzer
func RunAnalyzer() string {
	//kafka consumer

	//run pull、inspect、delete
	//timeout, write to redis, job id status is failed
	//write to mysql, image name、digest、layer

	//kafka producer, task id、job id、image name

	return ""
}
