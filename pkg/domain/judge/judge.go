package judge

import (
	"log"
)

// Judge
func Judge(imageName, imageDigest string) string {
	log.Println(imageName, imageDigest)
	//get data by imageName & imageDigest

	//if data is empty, run scan center

	//if status is running, return task id

	//if imageDigest is empty, return data

	//if imageDigest is db.imageDigest, run scan center

	//if imageDigest not is db.imageDigest, run scan center

	return ""
}
